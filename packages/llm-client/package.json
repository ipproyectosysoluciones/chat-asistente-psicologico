{
  "name": "@chatcap/llm-client",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAI-compatible client: chat (always temperature 0), NLI side-task, risk classification, and embeddings — model swap is configuration-only (REQ-RAG-1/5/7).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "build": "tsc -b tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@chatcap/config": "workspace:*",
    "@chatcap/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@types/node": "catalog:"
  }
}
