{
  "name": "@chatcap/config",
  "version": "0.1.0",
  "private": true,
  "description": "Zod-validated environment schema (fail-fast boot) and KeyProvider abstraction (pilot env impl, prod Vault/KMS swappable).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "build": "tsc -b tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@chatcap/shared-types": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@types/node": "catalog:"
  }
}
